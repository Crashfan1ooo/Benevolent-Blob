using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.EventSystems;

public class NavigatingMenu : MonoBehaviour
{
    /*
    public GameObject menuFirstButton;
    public GameObject keybindingMenu;
    public GameObject colorblindMenu;
    public GameObject storyMenu;
    public GameObject mainMenu;
    public GameObject keybindingFirstButton;
    public GameObject keybindingClosedButton;
    */
    // Start is called before the first frame update
    void Start()
    {
        //clears selected object
        //EventSystem.current.SetSelectedGameObject(null);

       
    }

    // Update is called once per frame
    void Update()
    {
        /*
        if(keybindingMenu.activeSelf == true)
        {
            EventSystem.current.SetSelectedGameObject(keybindingFirstButton);
        }

        if(mainMenu.activeSelf == true)
        {
            EventSystem.current.SetSelectedGameObject(menuFirstButton);
        }
        */
    }
}
